{
    "kind": "collectionType",
    "collectionName": "prayer_overrides",
    "info": {
        "singularName": "prayer-override",
        "pluralName": "prayer-overrides",
        "displayName": "Prayer Override"
    },
    "options": {
        "draftAndPublish": true
    },
    "attributes": {
        "type": {
            "type": "enumeration",
            "enum": ["iqama", "disabled"],
            "required": true
        },
        "prayerName": {
            "type": "string",
            "required": true
        },
        "startDate": {
            "type": "date",
            "required": true
        },
        "endDate": {
            "type": "date"
        },
        "iqama": {
            "type": "string"
        }
    }
}
